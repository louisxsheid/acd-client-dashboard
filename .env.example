# Auth.js Configuration
AUTH_SECRET=your-auth-secret-here
AUTH_TRUST_HOST=true

# Database Connection (use port 5433 for local docker-compose)
DATABASE_URL=postgres://postgres:postgres@localhost:5433/acd_client

# Hasura GraphQL Endpoint (use port 8081 for local docker-compose)
HASURA_GRAPHQL_ENDPOINT=http://localhost:8081/v1/graphql
HASURA_ADMIN_SECRET=devsecret

# Meilisearch Configuration (blazing fast search)
MEILISEARCH_HOST=http://localhost:7700
MEILISEARCH_API_KEY=devmasterkey

# Optional: Mapbox for premium geocoding
# VITE_MAPBOX_TOKEN=pk.xxx
