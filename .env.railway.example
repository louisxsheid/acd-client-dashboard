# Railway Environment Variables
# These should be set in the Railway dashboard

# Auth.js Configuration
AUTH_SECRET=${{secrets.AUTH_SECRET}}
AUTH_TRUST_HOST=true
ORIGIN=${{RAILWAY_PUBLIC_DOMAIN}}

# PostgreSQL (Railway provides these automatically when you add PostgreSQL plugin)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Hasura GraphQL Endpoint (use your Railway Hasura service URL)
HASURA_GRAPHQL_ENDPOINT=${{Hasura.RAILWAY_PUBLIC_DOMAIN}}/v1/graphql
HASURA_ADMIN_SECRET=${{shared.HASURA_ADMIN_SECRET}}

# Meilisearch Configuration
MEILISEARCH_HOST=${{Meilisearch.RAILWAY_PUBLIC_DOMAIN}}
MEILISEARCH_API_KEY=${{shared.MEILISEARCH_MASTER_KEY}}
